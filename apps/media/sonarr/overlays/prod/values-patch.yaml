apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: sonarr
  namespace: media
spec:
  values:
    image:
      tag: stable
    ingress:
      hosts:
        - host: sonarr.example.com
          paths:
            - path: /
              pathType: Prefix
