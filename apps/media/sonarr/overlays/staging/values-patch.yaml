apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: sonarr
  namespace: media
spec:
  values:
    image:
      tag: develop
    ingress:
      hosts:
        - host: sonarr.staging.example.com
          paths:
            - path: /
              pathType: Prefix
